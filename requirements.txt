# =============================================================================
# sqmg_project-qiskit — 環境依賴
# conda 環境名稱: qmg_qpso
# Python: >= 3.12（QMG 官方要求）
# =============================================================================
# 注意：此專案的 numpy >= 2.1.1，與 Project 1（CUDA-Q，numpy<2.0）環境完全分離。
# =============================================================================

# ── Qiskit 核心（對應 QMG README 指定版本）──────────────────────────────────
qiskit>=1.2.4
qiskit-aer>=0.15.1
qiskit-ibm-runtime>=0.30.0
qiskit-qasm3-import>=0.5.0

# ── 化學/分子生成 ─────────────────────────────────────────────────────────────
rdkit>=2024.3.5

# ── 數值計算（QMG 需要 numpy >= 2.1.1）──────────────────────────────────────
numpy>=2.1.1
pandas>=2.2.3

# ── 視覺化 ───────────────────────────────────────────────────────────────────
matplotlib>=3.9.2
seaborn>=0.13
pylatexenc>=2.10   # Qiskit 電路繪圖依賴

# ── 機器學習（選用，供未來與 BO 做 ablation study）───────────────────────────
scikit-learn>=1.5
